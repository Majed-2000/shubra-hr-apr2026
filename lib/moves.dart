// ============================================================================
// ملف: moves.dart
// الغرض: نموذج بيانات (DTO) يمثل صف حركة على المرتب (غياب، خصم، بدل، إلخ).
// متى يُستخدم: في شاشة emp_move.dart لعرض الحركات المؤثرة على الراتب.
// ============================================================================

/// One payroll adjustment row (absence, deduction, allowance, etc.).
/// [code] is the move-type identifier; [amount] is the monetary impact.
///
/// شرح الحقول:
/// - [name]:   اسم الحركة (مثلاً: غياب يوم، بدل سكن، خصم تأخير).
/// - [code]:   كود نوع الحركة في الـ backend.
/// - [date]:   تاريخ تطبيق الحركة على المرتب.
/// - [amount]: المبلغ المالي بالعملة (سالب للخصم، موجب للإضافة).
class Moves {
  String? name;
  String? code;
  String? date;
  String? amount;
}
